licenses(['notice'])

cc_library(
    name = 'eigen',
    visibility = ['//visibility:public'],
    hdrs = glob(['Eigen/**', 'unsupported/Eigen/**']),
    includes = ['.'],
    defines = [
        "EIGEN_INITIALIZE_MATRICES_BY_ZERO",
    ],
)
